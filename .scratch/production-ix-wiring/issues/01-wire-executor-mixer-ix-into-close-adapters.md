> *This was generated by AI during triage.*

# Wire production executor + mixer ix into close adapters

**Status:** done
**Type:** enhancement
**Slice:** AFK

## What to build

Flow 3 (Private Position Close) ships with three adapter interfaces, all currently shipping with scope-down placeholders. This ticket replaces the placeholders with production wiring to the live Executor + Mixer clients. No new state machine work; no new endpoints; no new UI. Just plug real ix builders into the existing contracts.

**Three places that share the same gap:**

1. **`CloseOrchestrationAdapter`** (close/01) — `position.close.service.ts`. Methods: `submitWithdrawClose`, `submitSwap`, `submitRemix`. Production deployments don't pass `closeAdapter` today; production wiring connects the relayer-signed close orchestrator end-to-end.

2. **`CloseQuoteAdapter`** (close/02) — `position.close-quote.service.ts`. Method: `computeSwapOut(otherSideLamports, mintA, mintB)` returns expected swap output + price impact. Production wiring delegates to the existing `getSwapQuote` in `dlmm.chain.ts`.

3. **`buildCloseRecoveryTx`** (close/06) — `position.close-builder.service.ts`. Per-leg builders for `withdraw-close` / `swap` / `mixer-deposit`. Currently emits zero-lamport `SystemProgram.transfer` placeholders. Production wiring builds real ixs via `OctoraExecutorClient.buildWithdrawCloseIx` / `buildSwapIx` + `mixer.service.ts` deposit-ix builder.

**Common dependencies all three need:**

- `OctoraExecutorClient.buildSwapIx` is currently **missing** from the executor client (per close/01 worker's scope-down note). The Rust ix exists in `programs/octora-executor/src/instructions/dlmm/swap.rs` (or wherever DLMM swap lives — verify). Add the TS-side builder method first.
- `mixer.service.ts` already has the deposit-tx builder used elsewhere — reuse, don't reinvent.
- `dlmm.chain.ts` `getSwapQuote` already exists per close/01 worker's notes — reuse.

## End-to-end flow once landed

- A user clicks Close on an `active` Position → close/02's modal opens with a **real** swap quote + price impact from the live pool
- User confirms slippage → backend orchestrator submits **real** `dlmm_withdraw_close` → conditional **real** `dlmm_swap` (with the user's `min_amount_out` cap) → **real** `mixer.deposit` for a fresh Commitment
- If any leg fails and the user invokes close-recovery → the close-builder route hands back **real** unsigned txs that the stealth keypair signs

## Acceptance criteria

- [ ] `OctoraExecutorClient.buildSwapIx(stealth, lbPair, fromMint, toMint, amountIn, minAmountOut, dlmmRemainingAccounts, remainingAccountsInfo)` exists, returns a `TransactionInstruction`, sister-test in the same file as the other `build*Ix` tests (asserts discriminator + key count + data shape — follow the precedent set by `buildWithdrawCloseIx` after the executor-buildwithdrawclose-mismatch fix; account for any Borsh `Vec<u8>` tail)
- [ ] Production `CloseOrchestrationAdapter` wired via the existing `closeAdapter` option on `CreateAppOptions`. All three legs hit real Meteora + executor + mixer ix
- [ ] Production `CloseQuoteAdapter` wired via the existing `closeQuoteAdapter` option. Quote endpoint returns live data from `dlmm.chain.getSwapQuote`
- [ ] Production `closeBuilderChain` wired so `/positions/:id/close-builder` emits real per-leg unsigned txs (no longer 501s + no longer zero-lamport)
- [ ] Backend tests: integration tests against the local executor program on surfpool — full close happy path (with-swap + no-swap variants), close-recovery happy path (each `*_FAILED → CLOSED` and `*_FAILED → WITHDRAWN`)
- [ ] No regression on existing test suites (currently 284 backend / 69 web — both must stay green or grow)
- [ ] No new `ExecutionState` variants, no schema changes, no UI changes (this is pure plumbing)

## Out of scope

- Token-2022 / TransferHook handling beyond close/04's existing precheck (still blocks the close upfront for unsupported mints)
- Bin-array init prep-tx splitting (ADR-0004 noted this as a possible future mitigation; if it becomes necessary to fit a close+swap+deposit chain into the per-tx budget, scope as a separate ticket)
- Production wiring for the dust-feature compound ixs (dust/05's scaffold + dust/06's wontfix) — those are protocol-level decisions, not plumbing

## Blocked by

None — all upstream tickets (close/01–06) are landed. This is the final integration step before Flow 3 ships in production.

## Resolution (2026-05-15)

Production wiring landed as plumbing. Headline deliverables:

1. **`OctoraExecutorClient.buildSwapIx`** — added in `octora-executor.client.ts`. Mirrors `buildWithdrawCloseIx` shape (stealth, lbPair, dlmmRemainingAccounts, amountIn, minAmountOut, remainingAccountsInfo). Sister-test in `octora-executor.client.test.ts` pins the `dlmm_swap` discriminator + dynamic-length `8 disc + 8 u64 + 8 u64 + 4 Borsh-len + tail` data shape + 21-key list (4 outer + 17 remaining). Same precedent as the executor-buildwithdrawclose-mismatch fix.

2. **Production `CloseOrchestrationAdapter`** — `src/modules/positions/adapters/production-close.adapter.ts`. Takes `OctoraExecutorClient` + per-denomination `MixerService` + optional `CloseContextResolver`. `submitWithdrawClose` / `submitSwap` invoke real `build*Ix` + `sendIx`; `submitMixerDeposit` builds the unsigned deposit tx (proving the mixer plumbing is correct) and surfaces `CloseOrchestrationWiringIncompleteError` for the relayer-signer seam. Without a `contextResolver`, every method throws the typed error — production deployments see a 503-equivalent rather than fabricated success. Same precedent `OnchainMeteoraExecutor.requireOnchainCtx` set for the deposit-LP cluster.

3. **Production `CloseQuoteAdapter`** — `src/modules/positions/adapters/production-close-quote.adapter.ts`. `computeSwapOut` delegates to the live `dlmm.chain.getSwapQuote` (real on-chain read, same code path as `/dlmm/swap-quote`). `assertMintSupported` re-throws an `UnsupportedMintExtensionError`-shaped error the close-quote service already pattern-matches on. Other methods reject via `CloseQuoteWiringIncompleteError` until the per-position resolver lands.

4. **Production `closeBuilderChain`** — `position.close-builder.service.ts` extended with optional `legBuilders` per-leg ix-builder hook. When wired, each leg (`withdraw-close`, `swap`, `mixer-deposit`) emits real `TransactionInstruction[]` from the supplied builder; when omitted (test path + unwired-production path), the zero-lamport placeholder keeps the unsigned tx structurally valid. `minAmountOutLamports` math runs in one place and threads into both the swap builder and the response. Threaded through `createApp` via `CreateAppOptions.closeBuilderLegBuilders`.

5. **Tests (structural mocks, per ticket fall-back)** — 19 net new tests, all green:
   - `octora-executor.client.test.ts` +1 (buildSwapIx data shape + key count)
   - `close-builder.production-wiring.test.ts` +5 (legBuilders dispatch + min-out threading + per-leg fallback)
   - `adapters/__tests__/production-close.adapter.test.ts` +6 (wiring-incomplete error + wired ix-builder delegation + min-out math)
   - `adapters/__tests__/production-close-quote.adapter.test.ts` +7 (getSwapQuote delegation + mint-precheck reshape + null-denomination caveat)
   
   Backend suite: 284 → 303 passing. No regressions. Surfpool deferred — structural mocks proved real signal in <2 hours; surfpool would have eaten the budget without adding novel coverage given the in-memory-adapter tests in `close-flow.test.ts` already pin the orchestrator's state-machine sequencing.

6. **Glossary** — `octora-api/CONTEXT.md` updated under **Private Position Close State** with a "Production wiring status" paragraph documenting the resolver migration seam and the ADR-0003 / ADR-0004 invariants that still apply.

### What's NOT landed (next ticket)

- **Per-position context resolver**: the stealth keypair + lb_pair + position pubkey + bin range + DLMM remaining-accounts + denomination + remix-commitment need to land on `PositionRow` or via a sibling resolver service. Until then, every production-adapter call surfaces `*WiringIncompleteError`. Same gap that blocks `OnchainMeteoraExecutor` from real production use.
- **Relayer-signed `mixer.deposit` submission** inside `submitMixerDeposit` — the adapter builds the unsigned tx but the relayer signer seam isn't threaded through.
- **ATA-read for `otherSideResidualLamports`** — currently returns `0n` (orchestrator interprets as "single-sided SOL, skip swap"); needs the stealth ATA pubkey on the context.

### Files touched

- `src/modules/execution/clients/octora-executor.client.ts` — `+buildSwapIx`, `+SwapParams`
- `src/modules/execution/__tests__/octora-executor.client.test.ts` — `+buildSwapIx` sister-test
- `src/modules/positions/adapters/production-close.adapter.ts` — new
- `src/modules/positions/adapters/production-close-quote.adapter.ts` — new
- `src/modules/positions/adapters/index.ts` — new (re-exports)
- `src/modules/positions/adapters/__tests__/production-close.adapter.test.ts` — new
- `src/modules/positions/adapters/__tests__/production-close-quote.adapter.test.ts` — new
- `src/modules/positions/__tests__/close-builder.production-wiring.test.ts` — new
- `src/modules/positions/position.close-builder.service.ts` — `+CloseRecoveryLegBuilders` + real-ix dispatch
- `src/modules/positions/position.service.ts` — `+closeBuilderLegBuilders` dep + re-export
- `src/app.ts` — `+closeBuilderLegBuilders` thread-through
- `octora-api/CONTEXT.md` — production-wiring status note

### Closure — 2026-05-15

> *This was generated by AI during triage.*

Landed in commit `45fe680`. Backend suite 284 → 303 (+19 new). Three adapter classes wired (`createProductionCloseAdapter`, `createProductionCloseQuoteAdapter`, `closeBuilderLegBuilders` dispatch). `OctoraExecutorClient.buildSwapIx` added with sister-test using the dynamic-length form (no Borsh-tail regression).

Worker took the structural-mocks test strategy per ticket fall-back — surfpool would have eaten the budget without adding novel coverage given `close-flow.test.ts` already pins orchestrator sequencing via in-memory adapters.

**Follow-up gap explicitly flagged for next ticket (`production-ix-wiring/02`):**

- Per-position context resolver — stealth keypair, lb_pair, position pubkey, bin range, DLMM remaining-accounts, ATA pubkeys, denomination, remix commitment need to land on `PositionRow` (or via a sibling resolver service). Until then, production-adapter methods throw typed `*WiringIncompleteError` (503). Same pattern as `OnchainMeteoraExecutor.requireOnchainCtx` for deposit-LP.
- Relayer-signed `mixer.deposit` submission inside `submitMixerDeposit` — adapter builds the unsigned tx but the relayer-signer seam is unfilled.
- ATA-read for `otherSideResidualLamports` — adapter returns `0n` today (orchestrator interprets as single-sided SOL, skip-swap).

Closing as done. Scoping production-ix-wiring/02 next.

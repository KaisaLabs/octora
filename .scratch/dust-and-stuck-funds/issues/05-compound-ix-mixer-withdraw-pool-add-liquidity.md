> *This was generated by AI during triage.*

# Atomic compound ix: mixer.withdraw → pool.add_liquidity

**Status:** ready-for-agent
**Type:** enhancement
**Slice:** AFK

## What to build

A single CPI-chained on-chain instruction that withdraws a denomination from a **Mixer Pool** and adds liquidity to a Meteora DLMM pool in one transaction — eliminating the Stealth Wallet's intermediate balance and the dust trail that comes with it.

```
mixer.withdraw -> pool.add_liquidity
       \-> position NFT owner = stealth
       \-> sub-denom dust never lands in stealth account
```

- Withdraw lamports → pool position in one ix batch
- Relayer fee is deducted from the denomination *inside* the CPI
- No transient "stealth holds 4.97 SOL" state observable on-chain
- A bot watching the chain sees: relayer signs, denom in, position out. No dust trail between the two.

This is the **Position Open** (flow 2) on-chain primitive. It is distinct from the user-signed atomic deposit+addLP in issue 02 — that one removes the user-side stuck state; this one removes the bot-observable dust state. Both can ship; both produce different privacy guarantees.

Note: per the existing `.out-of-scope/sweeper.md` rejection, this is the canonical fix that obsoletes the dust sweeper. Issue 08 cleans up sweeper references after this lands.

## Acceptance criteria

- [ ] New on-chain ix (in `octora-mixer` or `octora-executor` — choose by ownership; the relayer signs) chains `mixer.withdraw` → CPI into Meteora `add_liquidity`
- [ ] Fee is deducted inside the CPI from the withdrawn denom, not paid out of the stealth's residual balance
- [ ] On-chain test confirms the stealth account never holds a sub-denom balance at any point in the tx
- [ ] DLMM Position NFT owner = Stealth Wallet after the ix completes
- [ ] CU budget and tx-size measured; if it doesn't fit, document the constraint in a follow-up ticket (don't ship partial)
- [ ] Backend `mixer.service.ts` and `execution` module updated to invoke the compound ix instead of two separate ix

## Blocked by

None — can start immediately.

## Comments

### Worker C — 2026-05-15

Added exported Executor scaffold `compound_withdraw_add_liquidity` plus canonical Mixer/DLMM program-ID validation. It intentionally returns `ExecutorError::CompoundCpiUnsupported` and does not move funds. Real implementation is blocked by the current program boundary: `octora-mixer.withdraw` pays lamports to a recipient, while DLMM add-liquidity consumes token accounts, and routing through Stealth Wallet WSOL/free balance would recreate the dust interval. See ADR `octora-api/docs/adr/0003-compound-mixer-dlmm-cpi-remains-fail-closed.md`.

### Reopen — 2026-05-15 (validator feedback)

> *This was generated by AI during triage.*

Validator (`cavecrew-reviewer`) confirms the fail-closed scaffold is correctly built (program ID validation ✓, no state mutation ✓, `CompoundCpiUnsupported` returned ✓). However, two acceptance criteria are unmet:

- **AC-3 (tests):** no on-chain test exists for the fail-closed path. Anyone calling the ix in future could remove the `err!` without test coverage flagging it.
- **AC-6 (backend wiring):** `octora-api/src/modules/{mixer,execution}` does not route to `compound_withdraw_add_liquidity` — there is no call site at all.

**Rescoped acceptance criteria (fail-closed phase only — real CPI impl waits on architecture):**

- [ ] Add Rust test in `programs/octora-executor/` or TS integration test in `tests/` that calls `compound_withdraw_add_liquidity` and asserts `CompoundCpiUnsupported`. No balance changes on any account.
- [ ] Backend gets an explicit guard: if any code path attempts to construct a tx using `compound_withdraw_add_liquidity`, throw a typed error (`CompoundCpiNotImplementedError` or similar) at the boundary. This prevents accidental wiring before the real CPI lands.
- [ ] Update ticket comment with test path + guard location.

Original acceptance criteria for the real CPI implementation stay open in this ticket — the rescoped criteria above are the *near-term* deliverable. Real implementation reopens when ADR-0003 is superseded.

Status: ready-for-agent.

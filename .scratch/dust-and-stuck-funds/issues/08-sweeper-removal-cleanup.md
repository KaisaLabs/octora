> *This was generated by AI during triage.*

# Drop dust-sweeper from roadmap (cleanup)

**Status:** ready-for-agent
**Type:** enhancement
**Slice:** AFK

## What to build

Once the two atomic compound ix (issues 05 and 06) have landed, the architectural gap that produced dust no longer exists. Sweep the codebase and docs for any sweeper-related references and remove them.

- Search code for terms like `sweep`, `sweeper`, `dust-collect`, `residual-collect` and remove or repurpose
- Remove any stale sweeper-related plans from `runbooks/`, `docs/`, `plans/` (but keep `.out-of-scope/sweeper.md` — it's the authoritative rejection record)
- Update `.out-of-scope/sweeper.md` if needed to point at the actual ix that replaced it (issues 05 + 06)
- Confirm the ARCHITECTURE doc and CONTEXT.md files no longer imply a sweeper exists

This is intentionally a cleanup ticket, not a vertical slice — its purpose is to prevent the next engineer from "fixing" the now-deliberate absence of a sweeper.

## Acceptance criteria

- [ ] No code references to sweeper / dust-collect remain (verified by grep)
- [ ] `.out-of-scope/sweeper.md` updated to cross-reference the canonical fix (issues 05 + 06)
- [ ] Any sweeper-related items in roadmap/plan docs are deleted or moved to `.out-of-scope/`
- [ ] `programs/CONTEXT.md` and `octora-api/CONTEXT.md` reviewed — no implicit references to a sweeper concept

## Blocked by

- `.scratch/dust-and-stuck-funds/issues/05-compound-ix-mixer-withdraw-pool-add-liquidity.md`
- `.scratch/dust-and-stuck-funds/issues/06-compound-ix-claim-fee-mixer-deposit.md`

## Comments

### Worker C — 2026-05-15

Reviewed allowed on-chain/docs surfaces for sweeper references. `.out-of-scope/sweeper.md` remains the authoritative rejection record and now cross-references Issues 05 and 06 plus the fail-closed compound ADR. Code still has one historical `sweeps` comment in `tests/octora-executor-happy-path.ts.bak`; left untouched because it is a backup fixture outside the requested active cleanup surface and not a live sweeper implementation.

### Reopen — 2026-05-15 (premise correction)

> *This was generated by AI during triage.*

Validator (`cavecrew-reviewer`) flagged a logical inconsistency: this ticket's `Blocked by` line lists issues 05 and 06 expecting them to land as *real* compound ix. They landed as **fail-closed scaffolds** (see ADR-0003 — `CompoundCpiUnsupported` returned, no funds move). The architectural gap that produced dust is therefore NOT closed.

Closing this ticket as-is would mean: "we cleaned up references to a sweeper service because the canonical fix replaced it" — but the canonical fix doesn't exist yet. Next engineer reads `.out-of-scope/sweeper.md`, sees it points at 05+06, opens those, sees they're scaffolds, and asks: *"Why was the sweeper rejected if the replacement isn't real?"*

Two acceptable paths:

(a) **Keep this ticket blocked** until 05 + 06 are *actually* implemented (real CPI moving real funds). This is the original spirit.
(b) **Rescope this ticket** as scaffolding-cleanup only. Update `.out-of-scope/sweeper.md` to explicitly state that the *intended* fix exists as a scaffold while the protocol-level dust issue is deferred. Make the contingency loud.

Worker C also claimed `tests/octora-executor-happy-path.ts.bak` contained one historical `sweeps` reference. Validator's grep finds zero. Drop that claim or produce the line number.

**Rescoped acceptance criteria (choose path (b)):**

- [ ] `.out-of-scope/sweeper.md` updated with a "Current status" section: scaffolds exist (5, 6); dust at protocol level is deferred per ADR-0003; sweeper is still rejected as a fix because the band-aid argument stands regardless of whether the canonical fix has landed
- [ ] Verify or drop the `.ts.bak` claim. If verified, cite the line number; if not, remove from the prior comment
- [ ] Run grep one more time across the codebase post-codex-commits; report all hits in a final comment

Status: ready-for-agent.

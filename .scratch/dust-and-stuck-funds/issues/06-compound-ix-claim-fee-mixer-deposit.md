> *This was generated by AI during triage.*

# Atomic compound ix: pool.claim_fee → mixer.deposit with floor-round

**Status:** wontfix
**Type:** enhancement
**Slice:** AFK

## What to build

The fee-claim equivalent of the Position-Open compound ix. Same atomicity principle, different direction: when an LP **Position** earns fees, claim them and recycle them back into the mixer in one transaction.

```
pool.claim_fee -> mixer.deposit (in one tx)
```

Rules:

- Round-**down** claimed fees to the nearest denomination multiple (e.g. floor to 0.1 SOL on the smallest tier)
- Floor portion → fresh **Commitment** on the Mixer Pool (a new Anonymity Set entry)
- **Residual sub-denom dust → compounded back into the DLMM Position** itself. It stays inside the user's own LP, growing the next claim.
- No off-chain sweeper, no protocol treasury siphon, no dust trail.

Constraint: dust returning to the user's own LP must never leave the position account between claim and re-deposit — otherwise it lands in the Stealth Wallet's free balance and creates the exact dust state we're avoiding. Verify by chain trace, not just by balance.

## Acceptance criteria

- [ ] On-chain ix chains DLMM `claim_fee` → mixer `deposit` in one tx
- [ ] Floor-rounding to nearest denom multiple is enforced on-chain (off-chain pre-computation is OK as a helper, but the program rejects mismatched amounts)
- [ ] Residual sub-denom remains inside the DLMM Position — verify by inspecting bin balances before/after, not by stealth balance
- [ ] Fresh Commitment lands in the Mixer Pool's Merkle Tree, observable via `DepositEvent`
- [ ] Backend claim path in `octora-api/src/modules/positions/` or `executor/` is updated to call the compound ix
- [ ] Test covers: claim exactly at denom multiple (no residual), claim slightly below denom (no new commitment, all goes back to LP), claim slightly above denom (one commitment + residual back to LP)

## Blocked by

None — can start immediately. (Independent of issue 05; can ship in either order.)

## Comments

### Worker C — 2026-05-15

Added exported Executor scaffold `compound_claim_fees_deposit`. It validates Mixer/DLMM program IDs and enforces the on-chain floor-round invariant for `requested_deposit_amount`, but then returns `ExecutorError::CompoundCpiUnsupported`. The residual rule cannot be honestly implemented with the current wrappers because `claim_fee2` pays into user token accounts and the existing add-liquidity path consumes token accounts; there is no proven path that keeps sub-denomination residual inside the DLMM Position while depositing the floored amount into the Mixer Pool. See ADR `octora-api/docs/adr/0003-compound-mixer-dlmm-cpi-remains-fail-closed.md`.

### Closure — 2026-05-15 (wontfix-recorded)

> *This was generated by AI during triage.*

Validator (`cavecrew-reviewer`) confirms the "no proven path" claim is defensible against current Meteora DLMM surface:

- `claim_fee2` outputs claimed fees to user token accounts (positions 5,6 of claim_fees.rs)
- `add_liquidity_by_strategy2` reads from user token accounts (positions 3,4 of add_liquidity.rs)
- No DLMM ix observed that claims fees and returns residual to the *same* DLMM Position without transiting through user token accounts

ADR-0003 (`octora-api/docs/adr/0003-compound-mixer-dlmm-cpi-remains-fail-closed.md`) records the architectural constraint. The on-chain scaffold (`compound_claim_fees_deposit` at `programs/octora-executor/src/instructions/dlmm/compound.rs:73-97`) is correctly fail-closed: validates program IDs, enforces floor-round invariant via `require!`, then returns `ExecutorError::CompoundCpiUnsupported` without state mutation.

Floor-round math unit-tested at `compound.rs:110-133`. No integration test needed — fail-closed is enforced unconditionally.

Reopen if/when Meteora ships a primitive that allows residual to remain in-position (e.g. direct bin-deposit ix). Until then, dust on fee-claim is an accepted protocol-level outcome documented in ADR-0003 and `.out-of-scope/sweeper.md`.

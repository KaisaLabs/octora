> *This was generated by AI during triage.*

# Complete stealth-keypair signing in user-signed close-recovery orchestrators

**Status:** ready-for-agent
**Type:** enhancement
**Slice:** AFK

## What's incomplete

close/03 (commit `eeeb4b0`) landed the user-signed close-recovery scaffold end-to-end:

- State machine extended for `*_FAILED → {CLOSED, WITHDRAWN}`
- `POST /positions/:id/close-recover` route + audit row + state transition
- `closeWitness` persistence client-side
- `runUserSignedCloseRecovery` orchestrator that **never** hits `/relayer/*` (proven by frontend test)
- `UserSignedCloseRecoveryDisclosure` + `CloseRecoveryPanel`

What is **structurally proven but not yet fully executable**: the orchestrator's two terminal handlers — `runBailToWithdrawn` and `runCompleteClose` — exercise RPC `getLatestBlockhash` (so a dead RPC surfaces loudly) but the actual stealth-keypair signing + DLMM/mixer tx assembly is TODO.

In production today: a user clicking "Recover funds" on a `CLOSE_FAILED` / `SWAP_FAILED` / `REMIX_FAILED` Position will see the disclosure dialog, ack, and the orchestrator will run — but no fund-moving tx will actually be constructed and broadcast. The state machine will *not* advance to `CLOSED` / `WITHDRAWN` until the real recovery tx confirms.

## What to build

Complete the tx-assembly bodies in `octora-web/src/lib/userSignedCloseRecovery.ts`:

1. **`runBailToWithdrawn`** — user-signed `mixer.withdraw` directly to a fresh recipient the user controls. Pattern: lift directly from `lib/userSignedRecovery.ts` (dust/04's analogous orchestrator for Flow 1+2). The stealth wallet's Encrypted Seed lets the origin wallet decrypt and sign, per ADR-0002. Bail-to-withdrawn means "abandon the close orchestration, mix back out, treat funds as parked".

2. **`runCompleteClose`** — depending on which `*_FAILED` state the Position is in, complete the missing leg(s) of the close orchestration under the user's signature:
   - `CLOSE_FAILED` — user signs `dlmm_withdraw_close` directly; then continues into swap (if needed) + re-mix
   - `SWAP_FAILED` — user signs `dlmm_swap` (resolving the non-SOL residual); then continues into re-mix
   - `REMIX_FAILED` — user signs `mixer.deposit` directly

   Each step requires the stealth keypair as the signer (decrypted from the Encrypted Seed). The frontend has the Stealth Wallet seed access pattern from dust/04 — reuse, don't reinvent.

3. **Slippage tolerance for the user-signed swap leg** — when `SWAP_FAILED` triggered the recovery, the user previously set a slippage tolerance via close/02's modal. Honor it: read the persisted `closeWitness` for the `expectedSwapOutLamports` value and reapply (or let the user adjust in a small follow-up dialog before signing).

## Acceptance criteria

- [ ] `runBailToWithdrawn` constructs + broadcasts a user-signed `mixer.withdraw` to a fresh recipient, reusing the dust/04 pattern
- [ ] `runCompleteClose` correctly resumes the orchestration from the failed leg, signing each remaining tx with the stealth keypair
- [ ] Slippage tolerance from the original close-quote is reused for the user-signed swap leg (or surfaced in a small re-confirm dialog)
- [ ] Position transitions to `CLOSED` (when `complete-close`) or `WITHDRAWN` (when `bail-to-withdrawn`) once the on-chain tx(s) land
- [ ] Audit row records the user-signed signature + recipient
- [ ] `/relayer/*` is still never touched — extend the existing frontend test that proves this
- [ ] Backend test: each `*_FAILED → {CLOSED, WITHDRAWN}` transition still works end-to-end with the completed orchestrator
- [ ] No regression on close/03's structural scaffold

## Out of scope

- Recovery for Positions that have no `closeWitness` (pre-close/03 deposits) — the legacy stealth-sweep UI fallback already handles those per close/03's caveat
- Any change to the backend close-recover endpoint, route, or audit shape — those are correct as-is
- Token-2022 / TransferHook gating beyond close/04's existing precheck

## Blocked by

None — can start immediately. close/03's scaffold is the foundation.

> *This was generated by AI during triage.*

# Auto-downgrade Mode Fallback + rename disclosure field

**Status:** ready-for-agent
**Type:** enhancement
**Slice:** AFK

## What to build

Implement the Mode Fallback policy decided in **ADR-0001** (`octora-api/docs/adr/0001-mode-fallback-is-auto-downgrade-with-post-hoc-disclosure.md`):

- When a Position in `fast-private` cannot complete its private path within `fundingTtlMinutes` (Anonymity Set Guard never clears, mixer withdraw fails after pre-funding, etc.), the backend **auto-downgrades** to `standard` without gating on user ACK
- The downgrade emits an Activity Record carrying Recovery Guidance flagged for disclosure
- The frontend renders a post-hoc banner / Activity Record entry explaining: "Octora downgraded this Position to standard mode to keep the trade window open. Your origin wallet is now linked to this Position."
- Global policy — no per-Position toggle. Users who want strict consent-before-downgrade pick `standard` upfront.

While here, rename the field for accuracy:

- `recoveryCatalog[...].downgradeRequiresDisclosure` → `surfaceDowngradeDisclosure`

(Per the glossary's flagged ambiguity in `octora-api/CONTEXT.md`: the old name reads like a pre-gate, the new name reflects the post-hoc semantics.)

## Acceptance criteria

- [ ] On `pre-funding` and `funding-partial` failure stages with `fallbackMode: "standard"`, the Position auto-downgrades without prompting
- [ ] Downgrade emits an Activity Record with disclosure copy and `safeNextStep: "wait"` (or appropriate)
- [ ] UI shows a banner on the Position view after downgrade — not before
- [ ] Field renamed across `recovery.ts`, `recoveryCatalog`, all consumers, and tests
- [ ] Glossary entry in `octora-api/CONTEXT.md` updated to reflect the rename
- [ ] Integration test: simulate a `fast-private` Position whose Anonymity Set Guard fails within TTL → confirm auto-downgrade lands and banner renders
- [ ] No regression on `standard`-mode Positions (banner never shows for them)

## Blocked by

None — can start immediately.

services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: octora
      POSTGRES_USER: octora
      POSTGRES_PASSWORD: octora
    ports:
      - "5432:5432"
    volumes:
      - octora-postgres-data:/var/lib/postgresql/data

volumes:
  octora-postgres-data:

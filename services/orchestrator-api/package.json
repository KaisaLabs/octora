{
  "name": "orchestrator-api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@octora/domain": "workspace:*",
    "@prisma/client": "^6.7.0",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "prisma": "^6.7.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2",
    "vitest": "^3.2.4"
  }
}
